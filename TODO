- [x] on the left sidebar add a version tag this should match the dev tag or the release tag on git
      → Added version from VITE_APP_VERSION (set in vite.config.js) to bottom of left sidebar
- [x] on the join page, the join button should be centered
      → Made Connect and Join Invite buttons full-width (block) and centered in card actions
- [x] the search bar blends in too much with the background, so do the servers
      → Changed search bar variant from "solo" to "solo-filled" for more visible background
      → Increased server card background opacity (0.04→0.07) and border visibility (0.06→0.1)
- [x] when you select a movie/tv show and it asks you to resume the play button should be the same yellow color as the first play button
      → Play button in resume dialog already uses color="primary" (yellow). Added surface color to dialog card for better contrast.
- [x] the chat window is a bit too big and bulky it is a small area and there is too much padding, this includes text messages, and the settings above it.
      → Added density="compact" to sidebar header, switches, and list items. Reduced padding with py-0 on switch rows. Shrunk close button.
- [x] all toggle switches look ugly the toggle circle is too giant and there should be a yellow plex color to signify that something is enabled.
      → Added density="compact" to shrink switches, added color="primary" so enabled state shows yellow/amber
- [x] when down the tree of movies/tv ex home -> rooday east -> 3 (0 rock) the parenthesis show the part that gets cut off, this is bad ui
      → Breadcrumb items now truncate with ellipsis (max-width: 150px) instead of raw overflow clipping
